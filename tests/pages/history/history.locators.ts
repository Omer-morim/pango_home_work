
//  History page (`/history`) — completed sessions / audit trail.

export const historyLocators = {
// Scope assertions to main content when possible 
  main: 'main',
  tableBody: 'table tbody',
} as const;
